defaultEntryPoints = ["http", "https"]

[entryPoints]
  [entryPoints.dashboard]
    address = ":8080"
    [entryPoints.dashboard.auth]
      [entryPoints.dashboard.auth.basic]
        users = ["eddie:$apr1$Z1ExGZxw$zEmD0rANF0xxFmuKzT91R."]
  [entryPoints.http]
    address = ":80"
      [entryPoints.http.redirect]
        entryPoint = "https"
  [entryPoints.https]
    address = ":443"
      [entryPoints.https.tls]

[api]
entrypoint="dashboard"
dashboard = true

[docker]
endpoint = "unix:///var/run/docker.sock"
domain = "eisvogelweg2.de"
exposedByDefault = false
watch = true

[acme]
email = "eddie.faf@googlemail.com"
storage = "acme.json"
entryPoint = "https"
onHostRule = true
  [acme.httpChallenge]
entryPoint = "http"
